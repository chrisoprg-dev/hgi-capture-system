[build]
builder = "NIXPACKS"
buildCommand = "cd organism && npm install"
watchPatterns = ["organism/**"]

[deploy]
startCommand = "cd organism && node index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
